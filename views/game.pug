extends layout

block content
  h1= title
  <br>
   - let Create = `dispatch(\"/game/create\")`
   button.btn( class="btn-primary ml-1", onclick= Create) Create
  - let items = results;
  div(class="container-fluid")
    div(class="row")
      each item,index in items
        - let destination = `dispatch(\"/game/update?id=${item._id}\")`
        - let detail = `dispatch(\"/game/detail?id=${item._id}\")`
        - let Delete = `dispatch(\"/game/delete?id=${item._id}\")`
        div(class="col-sm-2" class="m-1")
          h3 #{item.brand}
          p #{item.color}
          p #{item.price}
          button.btn( class="btn-primary ml-1", onclick= destination) Update
          br
          br
          br
          button.btn( class="btn-warning ml-1", onclick= detail) Details
          br
          br
          button.btn( class="btn-danger ml-1", onclick= Delete) Delete
  script.
    function dispatch(path){
      console.log(path);
      window.location.href=path;
    }